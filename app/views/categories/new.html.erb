categoría
<div id="category_form">
  <%= form_for @category do |f| %>
    <table>
      <tr>
        <td>categoría:</td>
        <td><%= f.text_field :category %></td>
      </tr>
      <tr>
        <td></td>
        <td><%= f.submit 'crear',:class => "greenButton" %></td>
      </tr>
    </table>
  <% end %>
</div>
